// Array to store student scores
var scores = [85, 70, 95, 60, 80];

// Sorting the array in ascending order
scores.sort(function(a, b) {
    return a - b;
});

// Display the sorted array
console.log("Sorted scores in ascending order:", scores);
